rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DEV ONLY: open read/write. Tighten before production.
      allow read, write: if true;
    }
  }
}
