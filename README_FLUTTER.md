# แอปคำนวณราคาผลส้ม (Orange Calculator App)

แอปพลิเคชัน Flutter สำหรับจัดการข้อมูลและคำนวณราคาผลส้มประเภทต่าง ๆ

## ฟีเจอร์หลัก

- 📊 **หน้าแรก (Home Screen)**: แสดงภาพรวมของข้อมูลผลส้ม
- 📁 **ข้อมูลที่จัดเก็บ (Data Screen)**: ดูขนาดและมิติของผลส้มแต่ละชนิด
  - ความสูง
  - รัศมี
  - เส้นผ่านศูนย์กลาง
- 🧮 **คำนวณราคา (Calculator Screen)**: คำนวณราคาผลส้มตามน้ำหนัก

## ชนิดผลส้มที่รองรับ

1. **ส้มสายน้ำผึ้ง** - 45 บาท/กก.
   - รสชาติหวานฉ่ำ เนื้อนุ่ม น้ำมาก
   
2. **ส้มเขียวหวาน** - 35 บาท/กก.
   - หวานกรอบ สดชื่น ไม่เปรี้ยว
   
3. **ส้มแมนดาริน** - 55 บาท/กก.
   - หวานหอม ปอกง่าย เนื้อละเอียด

## การติดตั้งและรัน

### ความต้องการของระบบ

- Flutter SDK (3.0.0 หรือสูงกว่า)
- Dart SDK
- Android Studio / Xcode (สำหรับทดสอบบน emulator/simulator)
- VS Code หรือ IDE อื่น ๆ

### วิธีการติดตั้ง

1. Clone หรือ download โปรเจกต์

2. ติดตั้ง dependencies:
```bash
flutter pub get
```

3. รันแอป:
```bash
flutter run
```

### รันบน Platform ต่าง ๆ

**Android:**
```bash
flutter run -d android
```

**iOS:**
```bash
flutter run -d ios
```

**Web:**
```bash
flutter run -d chrome
```

**Windows:**
```bash
flutter run -d windows
```

## โครงสร้างโปรเจกต์

```
lib/
├── main.dart                      # จุดเริ่มต้นของแอป
├── models/
│   └── orange_type.dart          # โมเดลข้อมูลผลส้ม
└── screens/
    ├── home_screen.dart          # หน้าแรก
    ├── data_screen.dart          # หน้าข้อมูลผลส้ม
    └── calculator_screen.dart    # หน้าคำนวณราคา
```

## การพัฒนาต่อยอด

สามารถพัฒนาเพิ่มเติมได้ดังนี้:

1. เพิ่มฐานข้อมูล (SQLite/Hive) สำหรับบันทึกประวัติการคำนวณ
2. เพิ่มฟีเจอร์ส่งออกข้อมูลเป็น PDF
3. เพิ่มการ login/authentication
4. เพิ่มภาษาอังกฤษและภาษาอื่น ๆ
5. เพิ่มธีมสี Dark Mode
6. เชื่อมต่อกับ API สำหรับราคาแบบ real-time

## เทคโนโลยีที่ใช้

- **Flutter**: Framework สำหรับสร้าง cross-platform apps
- **Dart**: ภาษาโปรแกรมมิ่งของ Flutter
- **Material Design 3**: สำหรับ UI/UX

## License

MIT License - สามารถใช้งานได้อย่างอิสระ

## ผู้พัฒนา

Converted from Next.js/React to Flutter by GitHub Copilot

---

**หมายเหตุ**: ข้อมูลราคาและขนาดผลส้มเป็นตัวอย่างเท่านั้น ควรปรับปรุงให้เป็นปัจจุบันตามความเหมาะสม
